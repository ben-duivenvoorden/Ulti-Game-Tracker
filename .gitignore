# JetBrains IDE
.idea/

# Claude Code — local settings are machine-specific; settings.json (shared config) is committed if present
.claude/settings.local.json
node_modules

# Local-only working docs (plans, design notes, feedback). Kept on disk, not tracked.
docs/

# Combined single-SWA build output (landing + app), produced by scripts/build-site.mjs
dist-site/