{
  "name": "ultimate-api",
  "version": "0.1.0",
  "private": true,
  "description": "Azure Functions backend for the Ultimate Stat Tracker — append-only event log + game reads.",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "@azure/functions": "^4.6.0",
    "@azure/identity": "^4.5.0",
    "@azure/storage-blob": "^12.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "rimraf": "^6.0.1",
    "typescript": "~5.7.2"
  }
}
