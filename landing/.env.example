# URL the "Open the web app" links point to. Defaults to https://ultigametracker.com
# when unset. Set this if the app moves (e.g. when this landing page is published
# at the apex domain and the app lives on a subdomain).
# VITE_APP_URL=https://ultigametracker.com
