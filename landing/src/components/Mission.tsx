export default function Mission() {
  return (
    <section id="mission" className="border-t border-border/60 py-20 sm:py-24">
      <div className="mx-auto max-w-6xl px-5 sm:px-8">
        <p className="font-mono text-sm uppercase tracking-widest text-accent-2">Mission</p>
        <h2 className="mt-3 text-3xl font-bold tracking-tight sm:text-4xl">
          Capture the game as it happens
        </h2>
        <p className="mt-5 max-w-3xl text-lg leading-relaxed text-muted">
          Ultimate moves fast — Ulti Game Tracker keeps up. A tap logs each
          pass, turnover and goal as it happens, from any phone on the line. It
          works offline and syncs to the cloud, so the whole game is captured
          live, without anyone breaking stride.
        </p>
      </div>
    </section>
  )
}
