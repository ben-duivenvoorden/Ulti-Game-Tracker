# Build Log — Design / Code Sync Execution

**Date:** 2026-05-24
**Driver:** Outstanding work from [design ↔ code delta](2026-05-24-design-code-delta.md) and [league layer scoping](2026-05-24-league-layer-scoping.md).
**Mode:** Unattended progress — picking safe starting points, logging as I go, stopping short of high-risk changes (portrait migration, half-time/end-game confirmation flow).

---

## Execution plan

### Phase 1 — Docs (canonical vocabulary first)
- [x] **1.1** Update `validation-rules.md` — canonical event-type list, state machine, three pick modes, first-possession gating, splice validation
- [x] **1.2** Update `features.md` — F2 rosette model, F4 truncate-cursor + splice, Copy/Paste/Edit, Drawers, per-device prefs
- [x] **1.3** Update `screens.md` + `screen-states.md` — drop landscape field-orientation, document new screens, three pick screens, AdminDrawer / LogDrawer
- [x] **1.4** Update `architecture.md` — current state (three logs, client-only) vs target state (WebSocket sync), UST envelope
- [x] **1.5** Update `product-requirements.md` — current reality, bonus carve-outs, role model, settings shape
- [x] **1.6** Patch `wire-protocol.md` — `truncate` + `splice-block` added; `reorder-line` pending move; UST envelope documented
- [x] **1.7** New: `docs/requirements/competition.md` — first-cut spec from L1–L12

### Phase 2 — Small/safe code changes
- [x] **2.1** `reorder-line` moved from rawLog to per-device transient `lineOrderOverride`
- [x] **2.2** `recordingOptions.brick` toggle plumbed through types / store / canvas / GameSettings UI
- [x] **2.3** "Edit log" entry point exposed in LogDrawer during live game
- [x] `npx tsc -b` clean. `npx vitest run` — 143/143 passing.

### Phase 3 — Held / deferred (do not start here)
- Portrait migration — held pending snapshot branch (per Q7)
- Half-time / end-game confirmation prompts — UX-sensitive, better with review (per Q5)
- Re-enable AdminDrawer manual Half Time / End Game buttons — depends on Phase 3 confirmation prompt design
- Game roster mutation events on rawLog (`game-roster-add-player` etc.) — large new event surface, better with review
- Competition layer code (entity, role system, settings cascade) — large; needs auth (A3) first

---

## Progress log

### ✅ Drafted Myall reply email

- Saved at [`docs/feedback/2026-05-24-myall-email-draft.md`](2026-05-24-myall-email-draft.md).
- ~500 words; organised by what's changing / not / considering / already covered / architecture.
- Closing line invites pushback on the judgement calls (event-first flow, compound turnovers).
- Footer notes flag tone / length / BUML-vs-Parity naming for sender to adjust.

### ✅ 2.1 `reorder-line` → per-device transient

- `store.ts`: new state field `lineOrderOverride: Record<TeamId, PlayerId[]>` (initial `{ A: [], B: [] }`). Added to `partialize` and reset in `resetAllData`.
- `store.ts`: new exported helper `applyLineOverride(engineLine, override)` — gracefully degrades stale overrides (filters out ids no longer on the field, appends new arrivals in engine order).
- `store.ts`: `reorderActiveLine` and `swapLineSlots` rewritten — no longer append `reorder-line` events; they compute the effective order from `engineLine + lineOrderOverride`, apply the swap/move, and store the result on `lineOrderOverride`.
- `engine.ts`: `step` case for `reorder-line` is now a documented no-op (legacy compat for any persisted rawLogs that still carry these events).
- `engine.ts`: `resolveRawLog` always filters `reorder-line`; `keepReorderLine` flag and `ResolveOpts` interface removed (no callers care). `applySplice` filters `reorder-line` unconditionally too.
- `engine.ts`: `resolveLogForDerivation` kept as a named alias of `computeVisLog`'s walker (produces same output now; name preserved for call-site readability).
- `LiveEntry/index.tsx`: imports `applyLineOverride`; subscribes to `s.lineOrderOverride`; reorders `activePlayers` per the override before passing to Stage.
- `store.test.ts`: swapLineSlots tests rewritten — asserts the override updates and the rawLog is unchanged.
- ✅ `npx tsc -b` clean. ✅ `npx vitest run` — 143/143 passing.

### ✅ 2.3 Expose "Edit log" entry point during live game

- `LogDrawer.tsx`: added optional `onBeginEdit?: () => void` prop. When provided (and not in edit mode), a small pencil-icon button appears in the LogHeader between EVENT LOG and the Select button.
- LogHeader: extended with the optional Edit button; new minimal `EditIcon` SVG.
- `LiveEntry/index.tsx`: passes `actions.beginEdit` through to LogDrawer's `onBeginEdit`.
- Edit log is now reachable both from the GameOverBanner (existing path) and from any live game state via the LogDrawer (per [delta audit Q4](2026-05-24-design-code-delta.md)).
- ✅ `npx tsc -b` clean. ✅ `npx vitest run` — 143/143 passing.

### ✅ 2.2 Brick toggle in RecordingOptions

- `types.ts`: added `brick: boolean` to `RecordingOptions`; default `true` in `DEFAULT_RECORDING_OPTIONS`.
- `layout.ts`: added `brickShown?: boolean` to `BuildOpts`; replaced the two `ARC_ORDER_PULL_*` constants with a dynamic `pullArcOrder(brickShown, bonusShown)` helper. Legacy 360° proposals path also conditional on `brickShown`.
- `Stage.tsx`: added `brickShown: boolean` to `StageProps`; threaded into both `chipsForPlayer` (awaiting-pull rosette) and the open-chip-rect recomputation dep array.
- `LiveEntry/index.tsx`: passes `brickShown={recordingOptions.brick}` to Stage alongside `stallShown` / `bonusShown`.
- `GameSettings/index.tsx`: new Brick toggle in the Events column, between Pull Distance Bonus and Foul.
- Tests: layout.test.ts updated to be explicit about `brickShown` (was implicitly always-on). New test cases for all four combinations of brick × bonus.
- ✅ `npx tsc -b` clean. ✅ `npx vitest run` — 143/143 passing.

### ✅ 1.7 docs/requirements/competition.md — new

- New top-level spec for the Competition layer, capturing every decision from the scoping doc (L1–L12)
- Sections: Overview / C1 Entity shape / C2 Settings cascade (with `SettingPolicy<T>` TS shape) / C3 Membership / C4 Roles / C5 Access flow / C6 Game roster / C7 Persistence (`competitionsLog`) / C8 Archival / C9 UI surfaces / C10 Concurrent edits
- Open questions section retains auth, scoping shape, server, and out-of-scope items

### ✅ 1.6 wire-protocol.md — targeted patches

- Added refresh stamp at top
- Append-only section: added `truncate` and `splice-block` event types; dropped "no splicing" wording; flagged `reorder-line` as pending-removal-from-wire
- "What stays local" section expanded with `truncateCursor`, `swapSides`, `pillSize`, drawer expansion; `reorder-line` listed as pending move
- New section: UST clipboard envelope — documents the envelope shape, paste validation, and that it's the de facto sync unit pre-server

### ✅ 1.5 product-requirements.md — full rewrite

- Section 3 (Goals) — removed "Built using Google Stitch" (not the current stack)
- Section 4 (Non-Goals) — kept Bonus features carve-out; added ABBA tracking as a bonus
- Section 5 (Users) — added Admin and (planned) Viewer roles
- Section 6 (Confirmed Decisions) — restructured into thematic groups; updated to match reality (canvas + rosette, three pick modes, first-possession gating, auto-emit-vs-confirm divergence flagged, game-roster planned, ratio advisory not enforcement, settings shape documented)
- Section 7 — open Qs cleaned up; closed "league admin role" Q (yes — per L9b)
- Section 8 — full doc list including feedback docs

### ✅ 1.4 architecture.md — full rewrite

- Split into "Current state (as built)" vs "Target state (server-backed sync)" sections — was previously framed as if the server already existed
- Current state documents three append-only logs (`rawLog`, `teamsLog`, `scheduledGamesLog`), Zustand persist + migrations, single-recorder model, UST clipboard envelope
- Target state preserves the WebSocket protocol direction with offline-first as the locked invariant
- Game roster mutations called out as planned
- Competition layer flagged as a planned fourth log
- Open question about server tech retained; new open Q about Competition-scoped logs shape

### ✅ 1.3 screens.md + screen-states.md — full rewrite

**screens.md:**
- Title dropped "& Field Orientation"; field-orientation section removed entirely (per Q7)
- Portrait-first reaffirmed with snapshot-branch precondition called out
- Screen list expanded from 3 to 7: Game Setup / New Game form / Game Settings / Teams Manager / Line Selection / Live Event Entry / Game Over
- Screen 6 (Live Entry) rewritten — header + AdminDrawer + canvas + LogDrawer model; ASCII layout sketch
- Wireframe prompts section gutted (kept a placeholder; will be regenerated post-portrait)

**screen-states.md:**
- All states relabelled with numbered prefixes matching new screen numbering (1a/1b/1c for Game Setup, 3 for Settings, 4 for Teams Manager, 5a/5b for Line Selection, 6a–6j for Live Entry, 7 for Game Over)
- New states added for the three pick modes (6d Block / 6e Intercept / 6f Receiver Error), Truncate Preview (6g), Edit Mode (6h), Game Over (7)
- Stale 3d Receiver Error Pick "removed" note dropped — receiver-error is back as its own state
- Each Live Entry state now lists which drawer / header strip / canvas mode applies

### ✅ 1.2 features.md — full rewrite

- F1 expanded with team-roster (admin-managed, durable) vs game-roster (scorer-mutable, planned) distinction
- F2 rewritten — canvas + chip rosette model (not "explosion centre/left/right"); three pick screens; first-possession gating documented
- F3 mentions LogDrawer (F11); muted entries flagged
- F4 fully rewritten — append-only with four structural event types (undo / amend / truncate / splice-block); new sub-sections F4a Undo / F4b Truncate-cursor rewind / F4c Copy-Paste / F4d Edit mode
- F6 marked as aspirational (no server today)
- F7 explicitly documents the auto-emit-vs-confirm divergence (design wants confirm; code currently auto-emits)
- F11 (Drawers) added — AdminDrawer + LogDrawer
- F12 (Per-device display preferences) added — swapSides, pillSize, pill-reorder (pending move to transient), drawer expansion
- Deferred Features expanded — game roster, "?" player, in-app stats, field-location, Competition layer, ABBA tracking refresh

### ✅ 1.1 validation-rules.md — full rewrite

- Pass → Possession everywhere; documented the "passing implicit in possession" principle prominently
- Event Types split into four groups: play / game-flow / stoppage / structural
- New events documented: `point-start`, `possession`, `intercept`, `timeout`, `foul`, `pick`, `system`, `undo`, `amend`, `truncate`, `splice-block`
- `reorder-line` documented as "pending move to transient" rather than as a rawLog event
- State-machine names aligned with engine `GamePhase` (`pre-game` / `awaiting-pull` / `in-play` / `point-over` / `half-time` / `game-over`)
- Old state-transition table replaced with concise per-phase canRecord table
- New section: **First-possession gating** (Goal / Receiver Error chips disabled until 2+ consecutive possessions)
- New section: **Pick-mode screens** — three transient UiMode pickers (`block-pick` / `intercept-pick` / `receiver-error-pick`)
- New section: **Splice-block validation** — replaces F4's "target position" model with id-based references
- Integrity rules rewritten (rule 8 now describes append-only with the four structural event types)
- Open questions resolved where possible; "pick foul vs pick mode" naming collision noted as deferred cleanup

